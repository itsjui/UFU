#include <stdlib.h>
#include <stdio.h>

int main(){
    int A, B, resultado;
    scanf("%d", &A);
    scanf("%d", &B);
    resultado = (A + B);
    int square = resultado * resultado;
    printf("%d", square);
    return (0);
}